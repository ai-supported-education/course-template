# {{ id }} — {{ измеряемое наблюдаемое явление }}

Время: {{ 30–60 минут }}

## {{ Что здесь нельзя узнать без измерения }}

{{ Начните с наблюдения или конкурирующих объяснений, между которыми нельзя честно
выбрать из одной теории. Свяжите ситуацию с предыдущим evidence или знакомым
опытом аудитории. }}

{{ Сформулируйте измеряемый вопрос и объясните, почему raw artifact нужен раньше
вывода. Не выдавайте expected за уже увиденный результат. }}

## Результат и разрешённый scope

Вы соберёте {{ raw artifact }} в {{ localhost/simulation/owned lab }} и отделите
наблюдение от вывода. Разрешённые targets: {{ точный список }}. Любой другой target
находится вне scope.

## До запуска: expected

Assumptions: {{ условия среды }}. Ожидается {{ качественный/численный прогноз с
допуском }}; это прогноз, а не уже полученное измерение.

## Preflight и baseline

- [ ] {{ Инструмент/версия/права }} доступны.
- [ ] {{ Питание, место, link, recovery channel — применимое }} проверены.
- [ ] Baseline сохранён в `{{ baseline.txt }}`.
- [ ] Raw evidence не содержит secrets или чужие данные.
- [ ] Output paths ещё не существуют либо создан новый уникальный run directory;
      прежний raw evidence не будет перезаписан.

Stop немедленно, если {{ неожиданный target, потеря доступа, перегрев, нагрузка,
выход за лимит или другое предметное условие }}.

## Процедура

1. Сохраните `expected_recorded_utc` до запуска. Затем выполните
   {{ безопасная команда/действие и параметры }} с отдельным start marker.
2. Сохраните raw output в `{{ measurements.csv }}`, не исправляя его под expected
   и не перезаписывая evidence предыдущей попытки.
3. Запишите время, существенные параметры среды и аномалии в `observations.md`.
4. Повторите {{ число }} раз или объясните, почему повтор невозможен.

### Как читать результат

{{ Два разобранных synthetic/sample сценария, включая контрастный. Явно пометьте,
что это не измерения учащегося. }}

## Cleanup / rollback

1. {{ Остановить временный процесс/вернуть конфигурацию/удалить lab resource }}
2. {{ Post-check конечного состояния }}

## Evidence и inference

- Raw: `{{ measurements.csv }}` и `{{ baseline.txt }}`.
- Context: `observations.md` с параметрами среды.
- Inference: {{ ограниченный вопрос, на который отвечают данные }}.
- Binary artifact: рядом добавьте текстовый companion с форматом, происхождением и
  способом интерпретации; review packet включает только metadata и SHA-256.

## DONE

- [ ] Scope и preflight подтверждены до действия.
- [ ] Raw observed data сохранены отдельно от expected и inference.
- [ ] Stop conditions не нарушены либо срабатывание честно зафиксировано.
- [ ] Cleanup/rollback выполнен и post-check записан.
- [ ] Agent review подтвердил воспроизводимость evidence.

## Что можно спросить дальше

{{ Назовите следующий вопрос, который возникнет только после этого измерения, и
оставьте его за пределами текущего DONE. }}
