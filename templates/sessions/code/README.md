# {{ id }} — {{ короткий результат }}

Время: {{ 30–60 минут }}

{{ Начните сразу после названия со знакомого действия, результата предыдущей
карточки или небольшого фрагмента поведения. Покажите, что именно в нём нельзя
объяснить прежней моделью. Отдельный заголовок нужен только тогда, когда он называет
реальную предметную коллизию. }}

{{ Свяжите возникший вопрос с изменением, которое учащийся сделает сегодня. Если
соседняя сложность отложена, спокойно скажите об этом. Не добавляйте секцию
«Введение» или «Контекст» только ради структуры. }}

{{ Если opening содержит код, до marker объявите или объясните все центральные
identifiers/API. Покажите исходное состояние, событие и наблюдаемый результат. }}

<!-- content-review:opening:end -->

## Результат и границы

После карточки {{ наблюдаемое поведение }}. В scope входит {{ одна целевая идея }};
{{ соседняя тема }} намеренно остаётся за пределами.

## До начала

- {{ Уже известные concepts }}
- {{ Команда, которая подтверждает рабочий starter }}
- {{ Runtime/tool versions или ссылка на общий setup }}

## Почему это работает

{{ Причинная модель и data flow. Объясните механизм до API. }}

{{ До первого code block объясните роли незнакомых API и значимых имён. Сам
фрагмент должен содержать начальное состояние, действие и наблюдение. }}

### Пример 1

```{{ language }}
{{ минимальный пример }}
```

{{ Пошагово объясните наблюдаемое поведение. }}

### Пример 2: граница модели

```{{ language }}
{{ contrast example }}
```

{{ Объясните отличие и где ломается очевидная аналогия. }}

## Два правдоподобных неверных пути

1. `{{ ложное решение }}` — {{ причинно, почему не выполняет контракт }}.
2. `{{ ложное решение }}` — {{ причинно, почему лишь скрывает проблему }}.

## Задание

Измените {{ файлы/поведение }} так, чтобы {{ точный публичный контракт }}.

Ограничения:

- {{ что нельзя обходить }}
- {{ что менять не требуется }}

## Проверка и evidence

- Local: `{{ команда }}` проверяет {{ наблюдаемое поведение }}.
- Agent: {{ что остаётся в rubric }}.
- Evidence: {{ изменённые файлы и результат check; не вставляйте выдуманный log }}.

## DONE

- [ ] Starter был воспроизведён до изменения.
- [ ] {{ Обязательное поведение }} наблюдается.
- [ ] Все declared checks зелёные.
- [ ] Нет обязательного TODO следующей карточки.

## Куда дальше

{{ Одним естественным переходом назовите, на какой вопрос следующей карточки
выводит полученный evidence. Не требуйте начинать её сейчас. }}
