# {{ id }} — {{ вывод или вычисляемая величина }}

Время: {{ 30–60 минут }}

## Результат и область применимости

Вы получите {{ формула/оценка/алгоритм }} для {{ диапазон условий }} и проверите его
{{ размерностью, границей, контрпримером или независимым методом }}.

## Дано и принято

| Обозначение | Смысл | Единицы/тип | Статус |
| --- | --- | --- | --- |
| `{{ x }}` | {{ смысл }} | {{ unit }} | source fact / assumption |

Правило округления: {{ правило }}. Вне scope: {{ намеренная граница модели }}.

## Причинный вывод

1. {{ Откуда следует первый шаг }}
2. {{ Почему допустимо преобразование }}
3. {{ Что означает результат, а не только формула }}

### Пример 1

{{ Полный расчёт с промежуточными значениями и единицами. }}

### Пример 2: граница

{{ Нулевой/предельный/контрастный случай и его интерпретация. }}

## Типичные ошибки

1. {{ Потерянное допущение, единица или диапазон }}.
2. {{ Численно правдоподобный, но причинно неверный путь }}.

## Задание

Заполните `{{ worksheet.md / calculation.* }}`: покажите шаги, допущения,
округление, sanity check и краткий inference.

## Проверка и evidence

Evidence: {{ файл с выводом и промежуточными шагами }}. Команда `{{ local check }}`
(если она реализована) проверяет только {{ воспроизводимые свойства }}; agent допускает
альтернативный корректный вывод по rubric.

## DONE

- [ ] Все величины определены и имеют единицы/тип.
- [ ] Допущения и область применимости названы.
- [ ] Результат прошёл независимый sanity check.
- [ ] Inference не шире, чем позволяют исходные данные.
